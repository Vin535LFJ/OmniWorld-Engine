# Benchmarks

This directory will hold benchmark harnesses and reports. Current state: scaffold only.

## Required report metadata

OS, kernel, CPU, GPU, driver, CUDA, Vulkan SDK, TensorRT, Video Codec SDK/FFmpeg, compiler, build type, dataset/media/model, command, commit hash, and raw artifact path.
